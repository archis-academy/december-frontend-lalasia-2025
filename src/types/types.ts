export type ArticleCardProps = {
  date: string;
  title: string;
  category: string;
  imagePath: string;
  authorName: string;
  avatarPath: string;
  description: string;
};

export type Products = {
  id: string;
  title: string;
  subTitle: string;
  desc: string;
  image: string;
  price: number;
};
export type Slides = {
  title: string;
  image: string;
  subTitle: string;
  id: string;
  label: string;
};
